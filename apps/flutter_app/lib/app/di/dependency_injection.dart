class DependencyInjection {
  static Future<void> init() async {
    // Register global services, datasources, and repositories here.
    // Example: getIt.registerLazySingleton<AuthRepository>(() => AuthRepositoryImpl());
  }
}
